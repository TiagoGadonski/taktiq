namespace GymHero.Shared.Enums;

/// <summary>
/// Type of progress photo
/// </summary>
public enum ProgressPhotoType
{
    Before = 0,      // Initial photo (beginning of program)
    Progress = 1,    // Mid-program progress photo
    After = 2,       // Final photo (end of program)
    Milestone = 3    // Special milestone photo
}
