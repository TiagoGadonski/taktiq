namespace GymHero.Domain.Enums;

public enum VisibilityLevel
{
    Private = 0,        // Only owner can see
    FriendsOnly = 1,    // Only confirmed friends can see
    Public = 2          // Anyone can view and search
}
